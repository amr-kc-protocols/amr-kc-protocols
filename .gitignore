.claude/worktrees/pedantic-montalcini-c702b4/

# macOS junk
.DS_Store
**/.DS_Store

# test deps / artifacts
test/node_modules/
test/package-lock.json

# Provenance signing private key — never commit
VTA-PROVENANCE-PRIVATE-KEY.pem
*PRIVATE-KEY.pem
*.provenance.key

# Plaintext credentials roster — only the encrypted blob inside
# credentials-log.html belongs in the repo. roster.sample.json is
# fake demo data and is tracked on purpose.
roster.json
roster-*.json
!roster.sample.json
